### Copy this file to `.env` and fill values before running the stack.
### Do NOT commit your real `.env` — keep secrets out of git.

AIRFLOW_UID=XXX

# Spark connection URI (full URI with scheme)
SPARK_CONN='{"conn_type": "spark", "host": "spark://spark-master", "port": 7077, "extra": {"extra__spark__master": "spark://spark-master:7077", "extra__spark__deploy_mode": "client", "extra__spark__spark_binary": "spark-submit"}}'

# MinIO credentials (change locally)
MINIO_ACCESS_KEY=XXXXXX
MINIO_SECRET_KEY=XXXXXX
MINIO_ENDPOINT=http://minio:9000

# Bucket to write outputs to
BUCKET_NAME=uber-data-analysis

UBERUSER=XXXXXX
UBERPASS=XXXXXX
